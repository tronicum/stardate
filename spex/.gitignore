# Rust build output
/target

# Point cloud pipeline: your own local input files, and generated tilesets
/in
/out

# Graph pipeline: demos/ is local generated output (graph.json captures can
# contain real data from this machine — network paths, process names/PIDs,
# installed packages — so none of it goes into git history).
/demos/

# Viewer (Vite/TypeScript)
/viewer/node_modules
/viewer/dist

# OS / editor cruft
.DS_Store
*.swp

# Python cache (scripts/gen_*.py)
__pycache__/

# Local cache of real LDraw files fetched on demand by
# unibrick/gen_brick_demo.py — not committed; a deliberate full library
# mirror (if ever wanted) would be a separate, explicit git-lfs step
# instead, see BRICKs.md.
/unibrick/.ldraw-cache/
